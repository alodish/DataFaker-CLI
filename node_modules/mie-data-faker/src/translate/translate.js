import { faker } from '@faker-js/faker';


export function createTranslate(array) {
    const name = faker.helpers.arrayElement(array);
    return {
        name,
        trans_to: faker.random.alphaNumeric(24),
        trans_from: faker.random.alphaNumeric(24)
    }

};
